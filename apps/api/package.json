{
    "name": "api",
    "version": "0.0.0",
    "scripts": {
        "dev": "python run.py",
        "generate:requirements": "poetry export --without-hashes --format=requirements.txt > requirements.txt"
    }
}